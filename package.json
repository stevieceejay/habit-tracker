{
  "name": "habit-tracker",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.68.0"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:playwright": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
